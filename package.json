{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "learn run clean --stream",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "@types/node": "^17.0.19",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
