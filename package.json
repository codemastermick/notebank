{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "learn run clean --stream",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.19",
    "chai": "^4.3.6",
    "lerna": "^4.0.0",
    "mocha": "^9.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
